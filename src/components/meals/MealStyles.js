import {makeStyles} from "@material-ui/core";

export default makeStyles(() => ({
    mealCard: {
        maxWidth: "60rem",
        width: "90%",
        margin: "2rem auto",
        border: "1px solid grey",
        borderRadius :"20px",
    }

}))